//Input: ["h","o","l","a"]

// Output (String with comma delimited elements of the array in th same order): "h,o,l,a"

// My Solution:

export function printArray(array: number[]): string {
  return array.join(',')
}